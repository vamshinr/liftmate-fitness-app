// Smoke test placeholder. Real widget tests should mock Firebase + dotenv,
// which is out of scope for this stub.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(true, isTrue);
  });
}
